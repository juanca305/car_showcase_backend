PORT=5000
MONGO_URI=mongodb+srv://<USER>:<PASS>@cluster0.mongodb.net/car-rental?retryWrites=true&w=majority
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
ADMIN_API_KEY=supersecretadminkey

# Replace placeholders with real values in your local .env.

# ADMIN_API_KEY is a simple admin auth for early development (we’ll protect admin endpoints with it). Later you can replace it with JWT.
